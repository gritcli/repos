# DO NOT EDIT!
#
# This file is generated by the Terraform.
# See https://github.com/${org}/repos.

name: Continuous Integration

on:
  push:
  pull_request:
  schedule:
    - cron: 0 14 * * 0

jobs:
  ci:
    name: Shared
    uses: ${org}/repos/.github/workflows/shared-${workflow}.yml@main
    %{~ if length(secrets) > 0 }
    secrets:
    %{~ for key in secrets ~}
      ${key}: $${{ secrets.${key} }}
    %{~ endfor ~}
    %{~ endif ~}
